{
  "name": "ed-it",
  "version": "0.0.1",
  "description": "image editor",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel start-react start-python",
    "start-react": "cd frontend && npm start",
    "start-python": "cd backend && python server.py"
  }
  
,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
